apiVersion: v1
kind: Service
metadata:
    name: vijaymapipelinesdotnetcoredocker
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: vijaymapipelinesdotnetcoredocker